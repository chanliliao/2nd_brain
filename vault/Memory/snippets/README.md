# Snippets

## Purpose
Reusable code patterns, one-liners, and boilerplate with usage context.

## Entry format
Each entry lives in this folder as `YYYY-MM-DD_<slug>.md` or inline in `../../MEMORY.md` under the `## Snippets` section.

## Tags
`#memory/snippets`
