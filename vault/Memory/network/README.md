# Network

## Purpose
People met at events, online, or through work — context, how we met, last touchpoint. For personal relationships (family, close friends), see `../relationships/`.

## Entry format
Each entry lives in this folder as `YYYY-MM-DD_<slug>.md` or inline in `../../MEMORY.md` under the `## Network` section.

## Tags
`#memory/network`
